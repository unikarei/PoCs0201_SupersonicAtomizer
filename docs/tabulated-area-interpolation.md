# Tabulated Area Interpolation

Placeholder describing tabulated `(x, A)` interpolation behavior and boundary handling.

Required topics:

- piecewise linear interpolation
- behavior outside the supported domain
- strictly increasing `x` values required
- duplicate `x` values are invalid
- nonpositive area values are rejected
- must not silently extrapolate

TODO: define interpolation method and invalid-input rules.