# Case Configuration Models

This document lists the runtime case configuration model names created from YAML.

- `CaseConfig`
- `FluidConfig`
- `BoundaryConditionConfig`
- `GeometryConfig`
- `DropletInjectionConfig`
- `ModelSelectionConfig`
- `OutputConfig`

Each model is validated and translated from the raw YAML into typed runtime objects used by the application.