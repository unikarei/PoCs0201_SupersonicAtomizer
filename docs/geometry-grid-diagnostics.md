# Geometry/Grid Diagnostics

Placeholder describing geometry and grid checks executed before solver runs.

Required diagnostic topics:

- invalid axial range
- nonpositive area
- inconsistent table length
- duplicate `x` values
- fatal pre-solver errors

TODO: include invalid area checks and degenerate grid handling.