# Semantic Validation Rules

Placeholder for semantic validation topics and rules.

Required topics:

- required: key presence and types
- air or `steam` selection for working fluid
- SI units
- strictly positive numeric inputs where applicable
- fields: `droplet_diameter_mean_in`, `droplet_diameter_max_in`
- grid bounds: `x_start`, `x_end`, `n_cells`
- table checks: same length, strictly increasing `x`, `area_distribution.A`

TODO: document required semantic checks and examples.