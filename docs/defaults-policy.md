# Defaults Policy

This document records required default decisions and their rationale.

Required default keys and documented terms:

- `weber_critical`
- `standard_sphere`
- `outputs.generate_plots`
- `fluid.inlet_wetness`
- `outputs.output_directory`

Defaults must not be silently defaulted: must not be silently defaulted

TODO: expand default values and examples.